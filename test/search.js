
import { expect } from 'chai';

describe('Spotify', () => {
  describe('Smoke test', () => {
    it('Should exist the search, searchAlbums, searchArtists, searchTracks methods', () => {
      expect(2);
    });
  });
});
